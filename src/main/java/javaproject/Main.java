package javaproject;

//import java.util.Scanner;

public class Main {
  /*
   * public static void main(String[] args) {
   * Scanner sc = new Scanner(System.in);
   * SoC cpu = new SoC();
   * Interpreter inter = new Interpreter(cpu);
   * 
   * boolean loop = false;
   * 
   * System.out.println("Wpisz 'q', aby wyjsc");
   * System.out.print("Czy kontynuowac? (Y/N): ");
   * 
   * String startChoice = sc.nextLine();
   * if(startChoice.equalsIgnoreCase("Y")) {
   * loop = true;
   * }
   * 
   * while(loop) {
   * System.out.println("Stan procesora");
   * inter.statusReport();
   * 
   * System.out.println("Podaj komende w formacie: Opcode+tryb | Rd | Rs | IMM");
   * String input = sc.nextLine();
   * 
   * if(input.trim().equalsIgnoreCase("q")) break;
   * 
   * String fixedLine = Interpreter.optimize(input);
   * 
   * try {
   * inter.executeLine(fixedLine);
   * }
   * catch(Exception e) {
   * System.err.println("Error: " + e.getMessage());
   * sc.nextLine();
   * }
   * 
   * if(inter.getHLT()) break;
   * 
   * for(int i = 0; i < 10; i++) {
   * 
   * System.out.println("\n");
   * 
   * }
   * }
   * 
   * sc.close();
   * }
   */
}
